import React from 'react';
import { Stack, theme } from '@phobon/base';

import Footer from './Footer';

export default {
  component: Footer,
  title: 'Layout/Footer',
};

export const withDefaultProps = () => (
  <Footer />
);